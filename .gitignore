# IDE (rules and settings committed as part of template)
.claude/*
!.claude/rules/
!.claude/settings.json

# Local environment
.env.local

# Persisted data (bind mounts)
data/postgres/
data/uploads/

# Strapi auto-generated TS types (regenerated from schema.json on every start)
back/types/generated/
